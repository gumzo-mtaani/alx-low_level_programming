#include "main.h"

/**
 * _islower - checks lowercase letters
 * 
 * @c: character to check
 *
 * Return: 1 or 0 depending on the condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
