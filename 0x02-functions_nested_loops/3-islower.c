#include "main.h"

/**
 * _islower - int c
 *
 * Description: checks lowercase character c
 *
 * Return: Always 1 and 0 depending on the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'Z');
}
